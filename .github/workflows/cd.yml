name: CD

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: cd-peerly-gateway
  cancel-in-progress: false

env:
  IMAGE_NAME: peerly-gateway
  REGISTRY: ghcr.io/peerly-team

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-toolchain

      - name: Generate protobufs
        run: buf generate

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Test
        run: dotnet test --no-build --configuration Release

  build-push:
    needs: build-and-test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Build image
        run: |
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
          docker build -t "$IMAGE" .

      - name: Scan image for vulnerabilities
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          severity: CRITICAL,HIGH
          exit-code: 1
          ignore-unfixed: true
          vuln-type: os,library
          trivyignores: .trivyignore

      - name: Push image
        run: docker push "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"

  # =========================================================================
  # K3S — bump image tag in peerly-manifests; Argo CD picks up and syncs.
  # =========================================================================
  deploy-k3s-stage:
    needs: build-push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout peerly-manifests
        uses: actions/checkout@v4
        with:
          repository: peerly-team/peerly-manifests
          ref: main
          fetch-depth: 0
          token: ${{ secrets.MANIFESTS_PAT }}
          path: manifests

      - name: Bump gateway-stage image tag
        run: |
          cd manifests
          sed -i "s|^  tag: .*|  tag: ${{ github.sha }}|" values/gateway-stage.yaml
          git config user.email "ci-bot@peerly.local"
          git config user.name "peerly-ci-bot"
          if git diff --quiet values/gateway-stage.yaml; then
            echo "no change"
            exit 0
          fi
          git add values/gateway-stage.yaml
          git commit -m "stage(gateway): bump image to ${{ github.sha }}"
          for i in 1 2 3; do
            git pull --rebase && git push && exit 0
            sleep 5
          done
          echo "::error::failed to push after 3 attempts"
          exit 1

  deploy-k3s-prod:
    needs: deploy-k3s-stage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout peerly-manifests
        uses: actions/checkout@v4
        with:
          repository: peerly-team/peerly-manifests
          ref: main
          fetch-depth: 0
          token: ${{ secrets.MANIFESTS_PAT }}
          path: manifests

      - name: Bump gateway-prod image tag
        run: |
          cd manifests
          sed -i "s|^  tag: .*|  tag: ${{ github.sha }}|" values/gateway-prod.yaml
          git config user.email "ci-bot@peerly.local"
          git config user.name "peerly-ci-bot"
          if git diff --quiet values/gateway-prod.yaml; then
            echo "no change"
            exit 0
          fi
          git add values/gateway-prod.yaml
          git commit -m "prod(gateway): bump image to ${{ github.sha }}"
          for i in 1 2 3; do
            git pull --rebase && git push && exit 0
            sleep 5
          done
          echo "::error::failed to push after 3 attempts"
          exit 1
